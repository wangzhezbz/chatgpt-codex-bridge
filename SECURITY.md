# Security Policy

## 支持范围

安全修复优先进入最新的 0.x 版本。早期版本不保证回补。

## 报告安全问题

请不要在公开 Issue 中提交以下内容：

- ChatGPT Cookie、登录信息或 API Token
- `/api/config` 返回的 `apiToken`
- 私有项目、完整聊天记录或真实附件
- 本机用户目录、数据目录和可识别个人身份的信息

请通过仓库维护者提供的私密联系方式报告，并附上最小复现、影响范围和 CodexBridge 版本。

## 安全模型

- HTTP 服务默认仅监听 `127.0.0.1`。
- MCP、HTTP 和扩展必须使用匹配的项目、会话、线程和协议版本。
- Chrome 扩展只控制用户明确绑定的 ChatGPT 页面。
- 用户上传和 GPT 生成文件使用不同的捕获范围。
- 本地状态采用带锁原子写入和备份。

CodexBridge 依赖 ChatGPT 网页结构。发现页面结构不明或作用域不明确时，系统应停止操作，而不是猜测目标。
